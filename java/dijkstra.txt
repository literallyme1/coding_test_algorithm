비용이 있는 BFS


흐름

작은 순, 갈 수 있는 곳. 싼 곳 현재 cost + next cost -> 

static class Node{
    int to;
    int cost;

    Node(int to, int cost){
        this.to = to;
        this.cost = cost;
    }
}


List<Node> graph = new ArrayList[N+1];

for(int i = 1 i < N+1; i++){
    graph[i] = new ArrayList<>();
}

for(int[] r : road){
    int a = r[0];
    int b = r[1];
    int cost = r[2];

    graph[a].add(new Node(b, cost));
    graph[b].add(new Node(a, cost));
}


int INF = Integer.MAX_VALUE;

int[] dist = new int[N+1];
Arrays.fill(dist, INF);

PriorityQueue<Node> pq = new PriorityQueue<>((a, b) -> (return Integer.compare(a.cost, b.cost)));

dist[1] = 0;
pq.offer(new Node(1, 0));

while(!pq.isEmpty()){
    Node cur = pq.poll();

    int now = cur.to;
    int cost = cur.cost;

    if(cost > dist[now]){
        continue;
    } //이게 무슨의미지? 

    for(Node next : graph[now]){
        int nextCost = next.cost;

        if(cost < dist[next.to]){
            dist[next.to] = nextCost;
            pq.offer(new Node(next.to, nextCost));    
        }
    }
}

int answer = 0;
for(int i = 1; i )
