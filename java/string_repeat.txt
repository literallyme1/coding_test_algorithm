문자열처리

String str = "Hello world!";
str.length();
str.substring(1, 3);
str.replace("Hello", "hello");
str.equals(str2);
str.equalsIgnoreCase(str2);
str.contains("hello");
str.toLowerCase();
str.toUpperCase();
Character.isLetter(str);
Character.isDigit(str);
str.indexOf("o");
int num =Integer.parseInt("a", 2); //a를 이진수
String s = String.valueOf(num;
StringBuilder sb = new StringBuilder();

for(int i =0; i<str.length(); i++){
    str.charAt(i);
}
List<String> list = str.split(" ");

#신규 아이디 추천 

str = str.toLowerCase();
if(str.isLetterOrDigit() || ch == "-")