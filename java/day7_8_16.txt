# h -index
Arrays.sort(citations);

int n = citations.length;
for(int i = 0; i < n; i++){
    int count = n - i;

    if(citations[i] >= count){
        return count;
    }
}

int[] rightCount = new int[10001];
boolean[] leftCount = new boolean[10001];
int rightType = 0;
int leftType = 0;
for(int val : topping){
    if(rightCount[val]  == 0){
        rightType ++;
    }
    rightCount[val] ++;
}

for(int val : topping){
    지우고, bool 
}

#기능개발 


int deployDay = ((100 - progress[0]) + speed[0] -1 ) / speed[0];
int count = 1;
for(int i = 1; i < progress.length; i++){
    int day = (100 - progress[i] + speed[i]) / speed[i];
    if(deployDay >= day){
        count ++;
    }
    else{
        answer.add(count);
        deployDay = day;
        count = 1;
    }
}

# 뉴스 클러스터링 

List<String> list = new ArrayList<>();
for(int i = 0; i < str.length() -1 ; i++){
    list.add(str.substring(i, i + 2));
}


for(STring st : list){
    if(list2.contains(st)){
        list2.remove(st);
        intersection ++;
    }
}

int union = list1.size() + list2.size() - intersection;


int max = score.stream().max().getAsInt();
if(score 1 == max)

for(int val : arr){
    if(list.isEmpty()){
        list.add(val);
        continue;
    }
    if(list[list.get(size()-1)] != val){
        list.add(val);
    }
}