

priorityQueue
최대, 최소 를 계속 


PriorityQueue<Integer> pq = new PriorityQueue<>();
pq.offer();
pq.peek();
pq.poll();

pq.size();
pq.isEmpty();
PriorityQueue<Integer> pq = new PriorityQueue<>(Collections.reverseOrder());
PriorityQueue<Integer> pq = new PriorityQueue<>((a, b) -> { Integer.compare(b[1], a[1])});

int count = 0;


while(pq.peek() < k){
    int first = pq.poll();
    int seconde = pq.poll();

    int new = first + seconde *2;
    count ++;
    offer
}

#디스크 컨트롤러


PriorityQueue<int[][]> pq = new PriorityQueue<((a, b) -> { return Intger.compare(a[1], b[1])});

while (count <= job.length){
    while(index < arr.length && time >= arr[index][0]){
            index ++;
            pq.offer();

    }
    if(!pq.isEmpty()){
        pq.poll

        time +
        total
        conut
    }else{
        time = jobs[][];
    }

}

#야근지수 


pq = new priorityQueue<>(Collections.reverseOrder());

if( total < n){
    return 0;
}

while(n > 0){
    pollofrer
    n--;
}
