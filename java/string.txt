문자열처리
문자를 조회 변경


String str = "Hello World";
str.length();
str.contains("abc");
list<Sting> list = str.split(" ")

Character.isLetter(str);
Character.isDigit(str);

str.toLowerCase();
str.toUpperCase();
str.substring(2, 4); // 2부터 3까지
str.replace("Hello", "Hi");

for(i = 0; i < str.length; i++){
    str.charAt(i);
}

str2 = "Hello";
str.equals(str2);
str.equalsIgnoreCase(str2);

StringBuilder sb = new StringBuilder();


for(i = 0; i < str.length; i++){
    if(i > 0) sb.append(" ");
    sb.append(str.charAt(i));
}

str.toString();

str.toCharArray();
str.indexOf("abc");

int num = Integer.parseInt(str);
String s = String.valueOf(num);


#신규 아이디 추천

StringBuilder sb = new StringBuilder();

new_id = new_id.toLowerCase();

for (int i = 0; i < new_id.length(); i++) {
    char ch = new_id.charAt(i);

    if (Character.isLetterOrDigit(ch) 
    || ch == '-'
    || ch == '_'
    || ch == '.'
){
    sb.append(ch);
}
}


###문자열 처리 

for(int unit = 1; unit <= s.length() /2; unit++)

int end = Math.min(i + unit, s.length());
String current = s.substring(unit, end);

### 문자열 압축 
if(prev.equals(current)){
    count ++;
}else{
    if(count>0) sb.append(count);
    sb.append(prev);
    prev = current;
    count = 1;
}

###파일명 정렬 
while(start < s.length() && !Character.isDigit(s.charAt(start))){ start++; }
int end = start;

while( end < s.length() && )


int headCompare = headA.compareToIgnoreCase(headB);
if(headCompare != 0){ return headCompare;}
return headA.compareToIgnoreCase(numa, numb);


###n 진수 게임

String converted = Integer.toString(number, n).toUpperCase();
int index = (p - 1) + i * m;
answer.append(all.charAt(index))