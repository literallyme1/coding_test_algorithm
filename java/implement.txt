#구현 
- 직접 순서대로. 
- 조건에 따라 이동 
- 현재 위치 갱신 



for(route){
    int[] route.split(" ");
    char dir = 
    int dist = parts[1];

    if(dir = "r" )


    int nr = r; 
    int nc = c;

    for(i < dist){
        nr + dr
        nc + dc 

        if( || arr[] == 'X'){
            false
            break
        }
    }

    if(possible){
        r = nr
        c = nc 
    }
}


for(){
    for(){
        original[n][r] = arr[n - c - 1][r]; //90 도 회전 
    }

}
//뭐든 지 쓰고 시작학. 


상태 -> 조건 


//키패드 누르기 

for(int num : numbers){
    if([1,4 ,7].contains(num)){
        arr.l
        좌표
    }
    elif(){
        arr.2
        좌표
    }
    else{
        좌표 찾기 Math.abs(a1.x - b1.x) + abs()

    }
}


distance

x = (num - 1) / 3;
y = (num - 1) % 3;



//크레인 

for(c){
    for(r){
        if(0){
            continue
        }else{
            arr.add();
            arr[r][c] = 0
        }

    }
}


for(){
    if(이전 ){
        pop 
        count + 1
    }
}

for( ){
    charAt;
    if(isDigit){
        arr. Integer.parseInt()
        if(start != end){
            문자 바꾸기
            start = i + 1;
            end = i + 1;
        }

    }else{
        end = i;
    }
}