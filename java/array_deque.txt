앞 뒤에 넣거나 빼야한다. (stack, queue, deque)


Deque<Integer> deque = new ArrayDeque<>();

deque.offerFirst();
deque.offerLast();
deque.peekFirst();
deque.peekLast();
deque.pollFirst();
deque.pollLast();

deque.size();
deque.contains();
deque.isEmpty();


#괄호 문제

for(String value : str.toCharArray()){
    if(value == '('){
        deque.offerLast(value);
    }else{
        if(deque.isEmpty()){
            return false;
        }
        deque.pollLast();
    }
}

#프로세스
queue.offerLast(int[]{priority[i], i});
for (int[] process : queue) {
    if(process[0] > current[0]){
        higherExists = true;
        break;
    }
}

# 다리 건너는 트럭

currentWeight -= bridge.pollFist();

if(currentWeight + nextTruck <= weight){
    bridge.offerLast(nextTruck);
    currentWeight += nextTruck;
    index ++;
}else{
    bridge.offerLast(0);
}
