
#기능개발 
int deployDay = (100 - process[0]) + speed[0];
for(int i = 0; i < N; i ++){
    int day = (100 - process[i]) + speed[i];
    if(deployDay >= day){
        count ++;
    }else{
        answer.add(count);
        deployDay = day;
        count = 1;
    }
}

#뉴스 클러스터링 

#다중 집합 -> 하나씩 지움.

for(string val : str1){
    if(str2.contains(val)){
        str2.remove(String.valueOf(val));
    }
}

if(Character.isLetter(a) && Character.isLetter(b)){
    arr.append("" + a + b);
}

#모의고사

if (answer[i] == arr[arr1 % 5 + 1]){
    count ++; 
}

int max= Arrays.stream(score).max().getAsInt();
if(score 1 == max) list.add(1);


#같은 숫자는 싫어. 

for(int value : arr){
    if(list.isEmpty() || list.get(list.size() -1 ) != value){
        arr.add(value);
    }
}

#파일명 정렬

