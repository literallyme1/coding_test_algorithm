#삼각 달팽이 

dr = { -1, 0, 1};
dc = {0, 1, 0};


n = a(a + 1) /2;
r = 0;
c = 0;
d = 0;

for(int num = 0; num < n; i++){
    map[r][c]= num;
    int nr = r + dr[d];
    int nc = c + dc[d];

    if(0 > nr || nr >= n ||0 > nc || nc >= n || map[nr][nc] != 0){
        d = (n + 1) % 3;
        int nr = r + dr[d];
        int nc = c + dc[d];
    }  
    


}

# 행렬 테두리 회전 

x1 -= 1;
y1 -= 1;
x2 -= 1;
x2 -= 1;


for(int nc = x1; nc < x2; nc ++){
    int temp = arr[r][nc];
    int arr[][] = prev;
    prev = temp;

    min = Math.min(min, prev);
}

for(int nr = y1; i < y2; nr ++)

for(int nc = x2; i > x1; i --)

for(int nr = y2; i > y1; i --)

# 거리두기 확인하기 

for (int r = 0; r < 5; r++){
    for (int c =0; c < 5; c++){
        if(map[r][c] == 'P'){
            //pp
            if(map[r+1][c] == 'p' || map[r][c + 1] == 'p'){
                
            }

            //
        }
    }
}