중간에 넣거나, 조회

List<Integer> arr = new ArrayList<>();
arr.add(1); arr.add(2);
arr.get(1);
arr.set(0, 1);
arr.remove(0);
arr.remove(Integer.valueOf(1));

arr.contains(1);
Collections.sort(arr);


List<Integer> list = new ArrayList<>();

int deployDay = (100 - process[0] + speed[0] -1) / speed[0];
int count = 1
for(int i =0; i <process.length; i++){
    int day = (100 - process[i] + speed[i] -1) / speed[0];
    if (deployDay >= day){
        count ++;
    }else{
        list.add(count);
        count = 1; 
    }
}

