#충분히 툴 사용 가능. 하지만 직접 구현 

# from itertools import permutations

# n = int(input())

# numbers = list(range(1, n + 1))

# all_perms = permutations(numbers, n) # numbers에서 n개를 뽑는 모든 순열

# for perm in all_perms:
#     print(' '.join(map(str, perm)))  # perm은 (1, 2, 3) 형태의 튜플이므로, 공백을 두고 출력
 
